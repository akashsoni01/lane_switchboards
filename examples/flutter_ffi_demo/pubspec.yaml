name: flutter_ffi_demo
description: Lane messenger FFI smoke app (same .so / XCFramework as native).
publish_to: "none"
version: 0.9.2+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  lane_messenger:
    path: ../../bindings/flutter/lane_messenger
  ffi: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
