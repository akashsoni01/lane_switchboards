global:
  scrape_interval: 10s
  evaluation_interval: 10s

rule_files:
  - alerts.yml

scrape_configs:
  - job_name: lane_metrics_exporter
    static_configs:
      - targets: ["host.docker.internal:9090"]
        labels:
          env: local
